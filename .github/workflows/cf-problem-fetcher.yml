name: 🤖 Codeforces Problem Fetcher

on:
  discussion:
    types: [created]

jobs:
  fetch-cf-metadata:
    if: github.event.discussion.category.slug == 'codeforces-arena'
    runs-on: ubuntu-latest
    permissions:
      discussions: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install Dependencies
        run: pip install requests beautifulsoup4

      - name: Scrape Codeforces Problem & Sample Cases
        id: fetch_meta
        env:
          DISCUSSION_BODY: ${{ github.event.discussion.body }}
        shell: python
        run: |
          import os
          import requests
          import re
          from bs4 import BeautifulSoup

          body = os.getenv('DISCUSSION_BODY', '')
          
          # Updated Regex to match the new # header structure perfectly
          contest_match = re.search(r'# Contest ID\s*(?:\s*)?(\d+)', body, re.DOTALL)
          index_match = re.search(r'# Problem Index\s*(?:\s*)?([A-Z\d]+)', body, re.DOTALL)

          if contest_match and index_match:
              contest_id = contest_match.group(1).strip()
              index = index_match.group(1).strip().upper()
              url = f"https://codeforces.com/contest/{contest_id}/problem/{index}"
              
              print(f"Targeting URL -> {url}")
              
              # 1. Scrape the Problem Page for Statement & Samples
              page_response = requests.get(url)
              statement_text = "Description unavailable."
              samples_markdown = ""
              problem_name = f"Problem {index}"
              
              if page_response.status_code == 200:
                  soup = BeautifulSoup(page_response.text, 'html.parser')
                  
                  # Extract precise problem title
                  title_div = soup.find('div', class_='title')
                  if title_div:
                      problem_name = title_div.text.strip()
                  
                  # Extract a clean text overview of the statement description exclusively
                  header_div = soup.find('div', class_='problem-statement')
                  if header_div:
                      desc_paragraphs = []
                      # Loop only through description blocks, breaking before input/output specs
                      for child in header_div.find_all(recursive=False):
                          classes = child.get('class', []) or []
                          if any(c in classes for c in ['input-specification', 'output-specification', 'sample-tests', 'sample-test']):
                              break
                          if 'header' in classes:
                              continue
                          desc_paragraphs.extend(child.find_all('p'))
                      
                      if desc_paragraphs:
                          statement_text = "\n\n".join([p.text.strip() for p in desc_paragraphs[:3]])
                      else:
                          # Fallback to general paragraph pulling if layout varies
                          paragraphs = header_div.find_all('p')
                          if paragraphs:
                              statement_text = "\n\n".join([p.text.strip() for p in paragraphs[:3]])
                  
                  # Extract Sample Inputs & Outputs
                  sample_test_div = soup.find('div', class_='sample-tests')
                  if sample_test_div:
                      inputs = sample_test_div.find_all('div', class_='input')
                      outputs = sample_test_div.find_all('div', class_='output')
                      
                      for i in range(len(inputs)):
                          in_pre = inputs[i].find('pre')
                          out_pre = outputs[i].find('pre')
                          
                          if in_pre and out_pre:
                              in_text = in_pre.get_text(separator="\n").strip()
                              out_text = out_pre.get_text(separator="\n").strip()
                              
                              samples_markdown += f"#### 📥 Sample Input {i+1}\n```text\n{in_text}\n```\n"
                              samples_markdown += f"#### 📤 Sample Output {i+1}\n```text\n{out_text}\n```\n\n"

              # 2. Query API for Official Difficulty Ratings and Tags
              api_url = "https://codeforces.com/api/problemset.problems"
              api_response = requests.get(api_url)
              rating = "Unrated"
              tags = "`No tags assigned yet`"
              
              if api_response.status_code == 200:
                  data = api_response.json()
                  if data.get('status') == 'OK':
                      problems = data['result']['problems']
                      matched = next((p for p in problems if p.get('contestId') == int(contest_id) and p.get('index') == index), None)
                      if matched:
                          rating = matched.get('rating', 'Unrated')
                          # Extracts the clean problem title without the index prefix string
                          problem_name = matched.get('name', problem_name)
                          tags = ", ".join([f"`{t}`" for t in matched.get('tags', [])]) if matched.get('tags') else tags

              # 3. Assemble the Ultimate Markdown Template
              comment = (
                  f"## 🤖 Codeforces Automated Briefing\n\n"
                  f"🎯 **Problem Name:** [{problem_name}]({url})\n"
                  f"📊 **Difficulty Rating:** `{rating}`\n"
                  f"🏷️ **Algorithmic Tags:** {tags}\n\n"
                  f"### 📄 Problem Description Preview\n"
                  f"> {statement_text}\n\n"
                  f"### 🧪 Example Test Cases\n"
                  f"{samples_markdown}"
                  f"---\n"
                  f"*Use the discussion space below to break down optimization paths or link clean repository source files!*"
              )
              
              comment = comment.replace("$$$", "$")
              
              # Save the briefing card context text
              with open('comment_body.txt', 'w', encoding='utf-8') as f:
                  f.write(comment)
                  
              # Save the problem name to dynamically rename the discussion title component
              with open('problem_title.txt', 'w', encoding='utf-8') as f:
                  f.write(problem_name)
                  
              print("SUCCESS: Context files compiled completely.")
          else:
              print("ERROR: Parsing validation parameters dropped.")

      - name: Post Summary Comment to Discussion Thread
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DISCUSSION_ID: ${{ github.event.discussion.node_id }}
        run: |
          # 1. Dynamically rename the discussion header component to match the clean problem name
          if [ -f problem_title.txt ]; then
            PROBLEM_TITLE=$(cat problem_title.txt)
            gh api graphql -f query='
              mutation($discussionId: ID!, $title: String!) {
                updateDiscussion(input: {discussionId: $discussionId, title: $title}) {
                  discussion { id }
                }
              }' -f discussionId="$DISCUSSION_ID" -f title="$PROBLEM_TITLE"
            print "SUCCESS: Discussion renamed to $PROBLEM_TITLE"
          fi

          # 2. Inject the standard brief text block layout
          if [ -f comment_body.txt ]; then
            gh api graphql -f query='
              mutation($discussionId: ID!, $body: String!) {
                addDiscussionComment(input: {discussionId: $discussionId, body: $body}) {
                  comment { id }
                }
              }' -f discussionId="$DISCUSSION_ID" -F body=@comment_body.txt
          else
            echo "Skipping comment injection: No metadata generated."
          fi
